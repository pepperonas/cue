/** Typen für `app-version.mjs` — die Datei ist bewusst reines JS, weil sie von
 *  zwei Konfigurationen (vite, vitest) importiert wird, die vor jeder
 *  TypeScript-Übersetzung laufen. */
export const REPO_ROOT: string
export const APP_VERSION: string
